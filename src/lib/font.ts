import { Caveat, Protest_Revolution } from 'next/font/google';

export const caveat = Caveat({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
});

export const protestRevolution = Protest_Revolution({
  weight: '400',
  subsets: ['latin'],
});
